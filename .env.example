ARK_API_KEY=
ARK_AGENT_ID=
ARK_BASE_ENVIRONMENT_ID=

# 存储后端：auto（默认，按 REDIS_URL 推断）| memory（本地无 Docker 调试）| redis（强制，连不上即启动报错）
STORE_BACKEND=auto

# 使用 Redis 时填写；本地无 Docker 留空即可（auto/memory 模式会走进程内内存存储）
REDIS_URL=redis://127.0.0.1:6379
PORT=3000
